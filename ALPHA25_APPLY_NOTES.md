# v1.4.0-alpha.26 changed-files package

Copy these files over the repository root. Then run `./cleanup-alpha25-release-control-plane.ps1` in PowerShell to remove obsolete files. Alternatively apply the included patch from the companion `.patch` file.

Deleted files:

- `tests/changed-files-patch-hygiene-guard-lock-completion-check.mjs`
